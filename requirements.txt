# Pipeline config + domain models
pydantic>=2.6
PyYAML>=6.0

# AWS SDK + AgentCore deploy toolkit
boto3>=1.34
bedrock-agentcore-starter-toolkit

# Agent harness (runs inside Bedrock AgentCore Runtime; also runs locally)
strands-agents>=0.1.0

# Dev
pytest>=8.0
